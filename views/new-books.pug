doctype html
head
  title New Book
  link(rel='stylesheet', href='/stylesheets/style.css')
h1 New Book
form
  p
    label(for='title') Title
    input#title(name='title' type='text' onclick="focusInput()")
  p
    label(for='author') Author
    input#author(name='author' type='text' onclick="focusInput()")
  p
    label(for='genre') Genre
    input#genre(name='genre' type='text' onclick="focusInput()")
  p
    label(for='year') Year
    input#year(name='year' type='text' onclick="focusInput()")
  p
    input(type='submit' value='Create New Book' onclick="focusInput()")
  p
    a.button(href='all_books.html' onclick="focusInput()") Cancel